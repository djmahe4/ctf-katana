name: challenge-gen
version: "1.0.0"
description: |
  Intelligence-Augmented CTF Challenge Generator
  
  Automatically generates high-fidelity CTF challenges grounded in real-world 
  exploit and patch snippets, with AI-hardening to prevent trivial automated solving.

author: Purple Engine Team
license: MIT

category: research
tags:
  - ctf
  - challenge
  - generation
  - hardening

inputs:
  finding:
    type: object
    required: false
    description: Vulnerability finding to convert to challenge
  
  vuln_type:
    type: string
    required: false
    description: Vulnerability type for new challenge
  
  difficulty:
    type: string
    required: false
    default: medium
    enum: [easy, medium, hard, insane]
  
  category:
    type: string
    required: false
    enum: [web, pwn, crypto, forensics, reverse, misc, web3, mobile]
  
  ai_hardening:
    type: string
    required: false
    default: standard
    enum: [none, standard, aggressive]
  
  draft:
    type: object
    required: false
    description: Pre-generated challenge draft from agentic reasoning

outputs:
  status:
    type: string
  challenge:
    type: object
  files:
    type: array

kavach_policy: standard
