# Core dependencies
mcp>=1.23.0                # >=1.23.0 required: fixes DNS-rebinding + DoS CVEs
ollama>=0.4.0
pyyaml>=6.0
httpx>=0.25.0              # HTTP client used by agentic_dispatcher and fuzzing web_handler

# Purple Engine - Phase 1: CTFd Integration
requests>=2.31.0           # CTFd API client
beautifulsoup4>=4.12.0     # HTML parsing for CSRF tokens

# Purple Engine - Future Phases
# Phase 2: Kavach Security
psutil>=5.9.0             # Process monitoring for AutoEnforcer
watchdog>=3.0.0           # File system monitoring (optional)

# Phase 3: Research & RAG
chromadb>=0.4.0          # Vector database for RAG
# lancedb>=0.5.0           # Alternative vector DB

# Phase 3: Web scraping / research
playwright>=1.0.0         # Browser automation (research_chrome_scraper)
playwright-stealth>=1.0.0 # Anti-detection for Playwright

# Phase 4: Multi-Domain Support
# web3>=6.0.0              # Ethereum smart contract interaction
# pwntools>=4.11.0         # Binary exploitation tools
# frida>=16.0.0            # Android dynamic instrumentation

# Phase 4: Media & OCR (research_youtube_ocr)
opencv-python-headless>=4.0.0   # Video frame extraction
Pillow>=9.0.0                   # Image processing
pytesseract>=0.3.0              # OCR wrapper (requires system tesseract)

# Phase 4: Network forensics
scapy>=2.5.0             # Packet capture analysis (forensics skill)

# Development & Testing
pytest>=7.0
pytest-asyncio>=0.21
